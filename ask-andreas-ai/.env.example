# Anthropic API-Key für die Chat-Route (/api/chat).
# NUR serverseitig verwendet — landet nie im Client-Bundle.
# Lokal in .env.local eintragen (gitignored), auf Vercel als Environment Variable setzen.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
